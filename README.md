# How to Play

Interactive cheat sheets for learning tabletop games in minutes. Pick a vibe, skim the teach, and jump straight to the fun.

## Getting Started

```bash
npm install
npm run dev
# open http://localhost:3000
```

## 📁 Project Structure

```
├── app/                # Next.js app router entry points
├── data/               # Game teach sheets
├── public/             # Static assets (empty placeholder)
└── package.json        # Scripts & dependencies
```

## 🛠️ Available Scripts

- `npm run dev` – Start the dev server
- `npm run build` – Create a production build
- `npm start` – Serve the production build
- `npm run lint` – Lint with Next.js defaults

## Features

- Curated teach cards for four popular games
- Quick filters by category, complexity, and text search
- Detailed turn-by-turn breakdowns with table-ready tips
- Responsive layout with glassmorphism-inspired styling
